[project]
name = "chrome-vim-paste"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncio>=4.0.0",
    "playwright>=1.58.0",
]

[dependency-groups]
dev = [
    "ipython>=9.12.0",
]
